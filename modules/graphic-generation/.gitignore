.DS_Store
.env
.env.local
__pycache__/
*.pyc
.venv/
venv/

# Generated per run — not source.
logs/
output/

# Downloaded model weights, if they ever land here instead of the HF cache.
models/

# Model weights — never committed, never rsynced.
hf-cache/
.hf-cache/

# Staging file run_on_unicorn.sh copies in so rsync ships the captions.
# Transient - deleted at the end of every run.
.captions_batch.txt
