# OS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Env / secrets
.env
*.env.local

# Editors
.vscode/
.idea/

# Node (future UI)
node_modules/
dist/
build/

# tmp/ is TRACKED on purpose: it holds the extracted transcript slices we test
# external tools against and share with the team. They are small, and the exact
# bytes matter — "regenerate it from the script" is not the same artifact once a
# selection heuristic changes.

# Run logs — rule 3 tees every run here. Local record, not shared state.
logs/
