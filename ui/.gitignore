.venv/
__pycache__/

# Per-run FLUX intermediates. The boards themselves ARE tracked - these are the
# same images a second time, because an .excalidraw embeds every picture as
# base64 inside its JSON. Tracking both stored 7.5 MB of duplicates.
output/flux_*/

